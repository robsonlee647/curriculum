# Photo

Add your photo here as `photo.jpg` (square, ~500×500px works best).

Until that file exists, the page automatically shows a placeholder circle with your initials ("RL") instead — no code changes needed, just drop the file in.
